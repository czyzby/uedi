eclipse.project.name = 'uedi-java6'

dependencies {
  compile "com.github.czyzby:uedi-core:$libVersion"
  testCompile project(':api')
  testCompile files(project(':api').sourceSets.test.output.classesDir)
}
